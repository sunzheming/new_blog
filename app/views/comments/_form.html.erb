<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		Name:<br>
		<%= f.text_field :name, size: "75" %>
	</p>

	<p>
		Comment:<br>
		<%= f.text_area  :body, size: "75 x 10", placeholder: "say something..." %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>